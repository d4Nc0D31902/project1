import React from "react";
import { Box } from "@mui/material";
import MetaData from "./layout/MetaData";

const Home = () => {
  return (
    <Box sx={{ height: "100vh" }}>
      <MetaData title={"Home"} />
      Home
    </Box>
  );
};

export default Home;
